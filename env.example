# Environment Configuration
# Este projeto usa arquivos de configuração por ambiente:
# - .env.development para desenvolvimento
# - .env.production para produção
#
# Copie o arquivo correspondente:
# cp env.development.example .env.development
# cp env.production.example .env.production
#
# O arquivo será carregado automaticamente baseado na variável APP_ENV
# Por padrão, usa 'development' se APP_ENV não estiver definida

# Environment
APP_ENV=development

# App Settings
APP_NAME=FastAPI Project
APP_VERSION=1.0.0
DEBUG=True

# Database
# Formato: postgresql+psycopg2://user:password@host:port/database
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/minha_api_dev

# Security
# IMPORTANTE: Gere uma chave secreta forte para produção!
# Você pode gerar com: openssl rand -hex 32
JWT_SECRET_KEY=super-secret-dev
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS
# Para desenvolvimento, você pode usar ["*"] para permitir todos
# Para produção, especifique os domínios permitidos
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

